#pragma once

const int MAX_ITERATIONS = 1000;

const bool WIND_ENABLED = false;

// Índices: 0 = Norte (cima), 1 = Sul (baixo), 2 = Oeste (esquerda), 3 = Leste (direita)
const bool WIND_DIRECTIONS[4] = {true, false, false, false}; // Exemplo: vento sopra para norte e oeste
